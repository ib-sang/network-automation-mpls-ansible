---
- name: TEST PROCEDURE 
  hosts: sjc
  connection: local
  gather_facts: no
  vars:
    switch_role: "distribution"
    ansible_connection: netconf
    ansible_network_os: ios

  roles:
  - mp_bgp
  - routing_pe_ce
